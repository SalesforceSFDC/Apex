# Apex Calls to External Web Services

To make a from a trigger, call a class method that executes asynchronously.  
This method is called a future method and is annotated with @future(callout=true).
